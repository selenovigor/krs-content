# КРС · доска контент-задач

Генерируется из реестра сроков проекта: `build-person-board.mjs liza`, публикуется `publish-boards.sh`.
Руками `index.html` не править — перезапишется.

Публичная по ссылке, закрыта от поисковиков: `noindex` + `robots.txt`.
